Theme:
  type: "object"
  config:
    fields:
      id:
        type: "Uuid!"
      name:
        type: "String!"
      value:
        type: "String!"

ThemeInput:
  type: "input-object"
  config:
    fields:
      name:
        type: "String!"
      value:
        type: "String!"